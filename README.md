# 📊 Power Query Project – Restaurant Data Cleaning & Transformation

## 📌 Project Description

This project focuses entirely on data cleaning and transformation using Power Query in Power BI. The dataset represents a global chain of restaurants and includes detailed information about restaurant locations, cuisines, pricing, customer ratings, and votes. Alongside this, a separate country dataset is used to map country codes to their respective country names, enabling better geographic clarity in the data.

The transformation process begins with data inspection, where issues such as missing values, duplicate records, and inconsistent formatting are identified. Necessary cleaning steps are then performed, including removing null values, eliminating duplicates, and renaming columns to make them more meaningful and easy to understand.

One of the key challenges addressed in this project is handling columns with multiple values, such as cuisines. These values are split into separate columns (e.g., Cuisine 1, Cuisine 2, Cuisine 3) to enable better filtering and analysis. Additionally, data types are standardized to ensure consistency across numerical and text fields.

Another important transformation involves formatting the “Average Cost for Two” column by adding appropriate currency symbols, making the data more readable and aligned with real-world representation. Custom columns are also created where necessary to enhance the dataset.

The restaurant dataset is then merged with the country dataset using a common key (country code), enriching the data with proper country names. Irrelevant columns are removed, and filters are applied to retain only meaningful records.

## 💡 Skills Demonstrated

Through this project, I have significantly enhanced my skills in Power Query by applying practical data transformation techniques on real-world datasets. This includes data cleaning, column transformations, merging queries, handling complex data structures, and creating custom columns without relying on advanced coding.


## 🎯 Objective

The goal of this project is to demonstrate strong data cleaning and transformation skills using Power Query by converting raw, unstructured data into a clean and reliable dataset.

## 🚀 Outcome

The final output is a well-organized dataset that is consistent, accurate, and ready for further analysis or visualization, forming a strong foundation for data-driven decision-making.
